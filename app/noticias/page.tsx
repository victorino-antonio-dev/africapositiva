import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { blogPosts } from "@/data/blogPosts";
export const metadata: Metadata = { title: "Notícias e guias", description: "Informação sobre viagens, vistos, legalização, imigração e comunidade." };
export default function News() { return <><section className="bg-ink py-24 text-white"><div className="container-site"><span className="eyebrow">Centro de informação</span><h1 className="mt-6 max-w-3xl font-display text-5xl font-semibold sm:text-6xl">Notícias, guias e alertas úteis.</h1><p className="mt-5 text-stone-300">Conteúdo claro para decisões mais informadas.</p></div></section><section className="py-20"><div className="container-site grid gap-6 md:grid-cols-2">{blogPosts.map(p=><Link key={p.slug} href={`/noticias/${p.slug}`} className="group rounded-3xl border border-stone-200 p-8 hover:border-orange-300"><span className="text-xs font-bold uppercase tracking-wider text-orange-700">{p.category} • {p.read}</span><h2 className="mt-12 font-display text-2xl font-semibold group-hover:text-orange-700">{p.title}</h2><p className="mt-4 leading-7 text-stone-600">{p.excerpt}</p><span className="mt-7 inline-flex text-sm font-bold">Ler artigo <ArrowRight className="ml-2 h-4 w-4" /></span></Link>)}</div></section></>; }
